{
  "installCommand": "npm install --no-package-lock",
  "buildCommand": "npm run build -w client",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "https://YOUR-RENDER-URL.onrender.com/api/v1/:path*"
    }
  ]
}